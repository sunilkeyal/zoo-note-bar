# Home Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace editor empty state with a rich home page showing welcome hero, search bar, recent notes, and favorite notes.

**Architecture:** New `HomePage.tsx` component conditionally rendered in `page.tsx` when no `activeNoteId` is set. Removes empty-state logic from `MainArea.tsx`. Uses `useNotes()` for all note data and actions.

**Tech Stack:** Next.js App Router, React 19, shadcn/ui (card, button, input), Tailwind CSS 4, lucide-react, NoteContext

---

## File Structure

| File | Action | Responsibility |
|------|--------|----------------|
| `src/components/HomePage.tsx` | Create | Full home page: hero, search, recent/favorites sections |
| `src/app/page.tsx` | Modify | Conditional render: HomePage when no activeNote, else MainArea |
| `src/components/MainArea.tsx` | Modify | Remove empty-state block (no longer needed) |
| `docs/superpowers/specs/2026-06-30-home-page-design.md` | Reference | Design spec |

---

### Task 1: Create HomePage component

**Files:**
- Create: `src/components/HomePage.tsx`

- [ ] **Step 1: Write the component scaffold with hero section**

```tsx
"use client"

import React, { useState, useMemo } from "react"
import { useRouter } from "next/navigation"
import { useNotes } from "@/contexts/NoteContext"
import { cn } from "@/lib/utils"
import { Plus, Search, Clock, Star, FileText, ArrowRight } from "lucide-react"
import { Input } from "@/components/ui/input"
import { Button } from "@/components/ui/button"

export default function HomePage() {
  const { notes, loading, error, fetchNotes, activeNoteId, setActiveNoteId, createNote } = useNotes()
  const router = useRouter()
  const [searchQuery, setSearchQuery] = useState("")

  const filteredNotes = useMemo(() => {
    if (!searchQuery.trim()) return notes
    const q = searchQuery.toLowerCase()
    return notes.filter(
      (n) => n.title.toLowerCase().includes(q) || n.content.toLowerCase().includes(q)
    )
  }, [notes, searchQuery])

  const recentNotes = useMemo(() => {
    return [...filteredNotes]
      .sort((a, b) => new Date(b.updatedAt).getTime() - new Date(a.updatedAt).getTime())
  }, [filteredNotes])

  const handleCreateNote = async () => {
    const note = await createNote({ title: "Untitled" })
    if (note) setActiveNoteId(note._id)
  }

  const handleOpenNote = (id: string) => {
    setActiveNoteId(id)
  }

  if (loading) {
    return (
      <div className="flex-1 flex items-center justify-center bg-background">
        <p className="text-muted-foreground">Loading...</p>
      </div>
    )
  }

  if (error) {
    return (
      <div className="flex-1 flex items-center justify-center bg-background">
        <div className="text-center">
          <p className="text-muted-foreground mb-2">Could not load your notes.</p>
          <Button variant="outline" size="sm" onClick={fetchNotes}>Retry</Button>
        </div>
      </div>
    )
  }

  return (
    <div className="flex-1 overflow-auto bg-background">
      <div className="max-w-3xl mx-auto px-4 sm:px-6 py-8 sm:py-12 space-y-8">
        {/* Hero */}
        <div>
          <div className="flex flex-col items-center text-center">
            <div className="size-16 sm:size-20 rounded-2xl bg-primary flex items-center justify-center text-2xl sm:text-3xl font-bold text-primary-foreground shadow-sm mb-4 sm:mb-5">
              Z
            </div>
            <h1 className="text-2xl sm:text-3xl font-bold text-foreground">Welcome Back, Sunil</h1>
            <p className="mt-2 text-sm sm:text-base text-muted-foreground/60">
              Ready to capture your ideas?
            </p>
            <Button onClick={handleCreateNote} className="mt-6 gap-2">
              <Plus className="size-4 sm:size-5" />
              Create New Note
            </Button>
          </div>
        </div>

        {/* Search */}
        <div className="flex justify-center">
          <div className="w-full max-w-md relative">
            <Search className="absolute left-3 top-1/2 -translate-y-1/2 size-4 text-muted-foreground" />
            <Input
              placeholder="Search your notes..."
              value={searchQuery}
              onChange={(e) => setSearchQuery(e.target.value)}
              className="pl-10"
            />
          </div>
        </div>

        {/* Note sections */}
        <div className="space-y-8 sm:hidden">
          <NoteSection
            title="Recent Notes"
            icon={<Clock className="size-4 sm:size-5 text-muted-foreground" />}
            notes={recentNotes.slice(0, 3)}
            isFavorite={false}
            onOpenNote={handleOpenNote}
            onViewAll={() => router.push("/recent")}
          />
          <NoteSection
            title="Favorite Notes"
            icon={<Star className="size-4 sm:size-5 text-amber-500" />}
            notes={recentNotes.slice(0, 3)}
            isFavorite={true}
            onOpenNote={handleOpenNote}
            onViewAll={() => router.push("/favorites")}
          />
        </div>

        <div className="hidden sm:grid sm:grid-cols-2 gap-6">
          <NoteSection
            title="Recent Notes"
            icon={<Clock className="size-5 text-muted-foreground" />}
            notes={recentNotes.slice(0, 5)}
            isFavorite={false}
            onOpenNote={handleOpenNote}
            onViewAll={() => router.push("/recent")}
          />
          <NoteSection
            title="Favorite Notes"
            icon={<Star className="size-5 text-amber-500" />}
            notes={recentNotes.slice(0, 4)}
            isFavorite={true}
            onOpenNote={handleOpenNote}
            onViewAll={() => router.push("/favorites")}
          />
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Write the NoteSection sub-component**

Add below HomePage in the same file:

```tsx
function NoteSection({
  title,
  icon,
  notes,
  isFavorite,
  onOpenNote,
  onViewAll,
}: {
  title: string
  icon: React.ReactNode
  notes: Note[]
  isFavorite: boolean
  onOpenNote: (id: string) => void
  onViewAll: () => void
}) {
  if (notes.length === 0) {
    return (
      <div>
        <div className="flex items-center justify-between mb-3">
          <div className="flex items-center gap-2">
            {icon}
            <h3 className="text-sm font-semibold text-foreground">{title}</h3>
          </div>
        </div>
        <p className="text-xs text-muted-foreground py-4 text-center">
          {isFavorite
            ? "No favorite notes yet. Star a note to see it here."
            : "No recent notes yet. Create your first note above!"}
        </p>
      </div>
    )
  }

  return (
    <div>
      <div className="flex items-center justify-between mb-3">
        <div className="flex items-center gap-2">
          {icon}
          <h3 className="text-sm sm:text-base font-semibold text-foreground">{title}</h3>
        </div>
        <button
          onClick={onViewAll}
          className="inline-flex items-center gap-1 text-xs text-muted-foreground hover:text-foreground transition-colors"
        >
          View all
          <ArrowRight className="size-3" />
        </button>
      </div>

      <div className="space-y-1">
        {notes.map((note) => (
          <div
            key={note._id}
            onClick={() => onOpenNote(note._id)}
            className="flex items-start gap-3 p-3 rounded-lg border bg-card hover:bg-accent/50 transition-colors cursor-pointer"
          >
            {isFavorite ? (
              <Star className="size-4 text-amber-500 shrink-0 mt-0.5" />
            ) : (
              <FileText className="size-4 text-muted-foreground shrink-0 mt-0.5" />
            )}
            <div className="flex-1 min-w-0">
              <div className="flex items-center justify-between gap-2">
                <span className="text-sm font-medium text-card-foreground truncate">
                  {note.title || "Untitled"}
                </span>
                <span className="text-[10px] text-muted-foreground/60 shrink-0">
                  {formatRelativeTime(note.updatedAt)}
                </span>
              </div>
              <p className="text-xs text-muted-foreground line-clamp-2 mt-0.5">
                {stripHtml(note.content) || "No content"}
              </p>
            </div>
          </div>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Add helper functions**

Add before the HomePage component:

```tsx
function formatRelativeTime(dateStr: string): string {
  const now = Date.now()
  const date = new Date(dateStr).getTime()
  const diffMs = now - date
  const diffSec = Math.floor(diffMs / 1000)
  const diffMin = Math.floor(diffSec / 60)
  const diffHr = Math.floor(diffMin / 60)
  const diffDay = Math.floor(diffHr / 24)
  const diffWeek = Math.floor(diffDay / 7)

  if (diffSec < 60) return "just now"
  if (diffMin < 60) return `${diffMin}m ago`
  if (diffHr < 24) return `${diffHr}h ago`
  if (diffDay < 7) return `${diffDay}d ago`
  if (diffWeek < 4) return `${diffWeek}w ago`
  return date.toLocaleDateString()
}

function stripHtml(html: string): string {
  return html.replace(/<[^>]*>/g, "").trim()
}
```

- [ ] **Step 4: Add the Note import**

```tsx
import { Note } from "@/types"
```

- [ ] **Step 5: Verify component compiles**

Run: `npx tsc --noEmit --pretty 2>&1 | Select-String -Pattern "HomePage"`

Expected: no output (no errors)

- [ ] **Step 6: Commit**

```bash
git add src/components/HomePage.tsx
git commit -m "feat: create HomePage component with hero, search, and note sections"
```

---

### Task 2: Wire HomePage into page.tsx

**Files:**
- Modify: `src/app/page.tsx`

- [ ] **Step 1: Update page.tsx with conditional rendering**

```tsx
"use client"

import React from "react"
import { useSession } from "next-auth/react"
import { useRouter } from "next/navigation"
import { SidebarProvider, SidebarInset } from "@/components/ui/sidebar"
import AppHeader from "@/components/AppHeader"
import NotesSidebar from "@/components/NotesSidebar"
import MainArea from "@/components/MainArea"
import HomePage from "@/components/HomePage"
import { useNotes } from "@/contexts/NoteContext"

export default function Home() {
  const { data: session, status } = useSession()
  const router = useRouter()
  const { activeNoteId } = useNotes()

  React.useEffect(() => {
    if (status === "unauthenticated") {
      router.replace("/login")
    }
  }, [status, router])

  if (status !== "authenticated") {
    return null
  }

  return (
    <SidebarProvider>
      <NotesSidebar />
      <SidebarInset className="overflow-hidden">
        <AppHeader />
        {activeNoteId ? <MainArea /> : <HomePage />}
      </SidebarInset>
    </SidebarProvider>
  )
}
```

- [ ] **Step 2: Verify page.tsx compiles**

Run: `npx tsc --noEmit --pretty 2>&1 | Select-String -Pattern "page.tsx"`

Expected: no output (no errors)

- [ ] **Step 3: Commit**

```bash
git add src/app/page.tsx
git commit -m "feat: wire HomePage into page.tsx, show when no active note"
```

---

### Task 3: Remove empty state from MainArea

**Files:**
- Modify: `src/components/MainArea.tsx`

- [ ] **Step 1: Remove the no-activeNote empty state block**

Find and replace this in `MainArea.tsx`:

```tsx
  if (!activeNote) {
    return (
      <div className="flex-1 flex items-center justify-center bg-background">
        <p className="text-muted-foreground">Select a note or create a new one</p>
      </div>
    )
  }
```

Replace with nothing — MainArea is never rendered when there's no active note.

- [ ] **Step 2: Verify MainArea compiles**

Run: `npx tsc --noEmit --pretty 2>&1 | Select-String -Pattern "MainArea"`

Expected: no output (no errors)

- [ ] **Step 3: Commit**

```bash
git add src/components/MainArea.tsx
git commit -m "refactor: remove empty state from MainArea (handled by HomePage)"
```

---

### Task 4: Verify the full app

**Files:**
- Verify: `src/app/page.tsx`, `src/components/HomePage.tsx`, `src/components/MainArea.tsx`

- [ ] **Step 1: Full TypeScript check**

Run: `npx tsc --noEmit --pretty`

Expected: no errors

- [ ] **Step 2: Run existing tests**

Run: `npm test`

Expected: all tests pass

- [ ] **Step 3: Start dev server and manual checks**

Run: `npm run dev`

Checklist:
- [ ] No note selected → home page appears with hero, search bar, recent/favorite sections
- [ ] Click "Create New Note" → note created, editor opens
- [ ] Click a note card → editor opens with that note
- [ ] Click "View all" on Recent → navigates to `/recent`
- [ ] Click "View all" on Favorites → navigates to `/favorites`
- [ ] Search filters notes in both sections
- [ ] Clear search → all notes reappear
- [ ] Click "Home" in sidebar (with note selected) → deselects note, home page appears
- [ ] Resize to mobile width → sections stack vertically
- [ ] Resize to desktop width → sections appear side by side

- [ ] **Step 4: Commit any fixes**

```bash
git add -A
git commit -m "fix: address review findings"
```

---

## Self-Review

- [ ] **Spec coverage:** Hero with logo + "Welcome Back" ✓, CTA button ✓, search bar ✓, Recent/Favorites sections ✓, View all links ✓, clickable note cards ✓, loading/empty/error states ✓, responsive layout ✓
- [ ] **Placeholder scan:** No TBD/TODO in plan ✓
- [ ] **Type consistency:** Note type used matches `@/types` ✓, useNotes hook matches context interface ✓
